void setup()
{
  size(screen.width * 0.7,screen.height*0.7);
  background(20);
  fill(255);
  noLoop();
  PFont fontA = loadFont("courier");
  textFont(fontA,14);
}

void draw(){
  text("Hello Web",20,20);
  println("Hello ErrorLog!");
}